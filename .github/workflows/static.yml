name: Test and deploy to Pages
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run simulate
        env:
          GAMES: 50
      - name: Diagnose Devonian mutation interactions
        run: node scripts/devonian-mutation-diagnostics.js
        env:
          DEVONIAN_GAMES: 8
          DEVONIAN_START_GAME: 9
          DEVONIAN_SCENARIOS: baseline,no_omnivore,no_cutaneous,parasitism,regeneration,resistance_regeneration,cannibalism,scavenger,venom,camouflage_carapace
          DEVONIAN_LIMIT: 600
          DEVONIAN_TAIL: 100
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        if: github.ref == 'refs/heads/main'
        with:
          path: dist
  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: test
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/configure-pages@v5
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
