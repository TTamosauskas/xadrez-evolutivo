{
  "name": "xadrez-evolutivo",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "simulate": "node tests/simulate.js",
    "benchmark:periods": "node scripts/period-benchmark.js",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "prettier": "^3.6.2"
  }
}
